{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(rm -rf /)",
      "Bash(rm -rf .)",
      "Bash(git push origin main)",
      "Bash(git push origin master)",
      "Bash(git push --force)",
      "Bash(git reset --hard)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); cmd=$(echo \"$input\" | jq -r .tool_input.command 2>/dev/null); if echo \"$cmd\" | grep -qE \"rm\\s+-rf\\s+[/\\.*]|git\\s+push\\s+(--force|origin\\s+(main|master))|git\\s+reset\\s+--hard\"; then echo \"Blocked dangerous command: $cmd\" >&2; exit 2; fi'"
          }
        ]
      }
    ]
  }
}
